#!/usr/bin/python
class configuration:
	def __init__(self, _uname, _pwd, _ip, _host, _port):
		self.username = _uname
		self.password = _pwd
		self.ip       = _ip
		self.host     = _host
		self.port     = _port
